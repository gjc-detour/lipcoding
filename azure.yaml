name: lipcoding-productivity
metadata:
  template: lipcoding-productivity@0.0.1
services:
  web:
    project: .
    language: ts
    host: containerapp
    docker:
      path: Dockerfile
      remoteBuild: true
  # worker: disabled until azd provision creates the worker Container App
  # Uncomment after running: azd provision -e dev
  # worker:
  #   project: .
  #   language: ts
  #   host: containerapp
  #   docker:
  #     path: worker/Dockerfile
  #     remoteBuild: true
